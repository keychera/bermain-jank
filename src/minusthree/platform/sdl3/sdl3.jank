(ns minusthree.platform.sdl3.sdl3
  (:include
   "SDL3/SDL.h"
   "extra.hpp")
  (:require
   [minusthree.platform.sdl3.learn.learn02-texquad :refer [texquad]]))

(defn start
  ([] (start {}))
  ([{:keys [stop-atom]}]
   (texquad stop-atom)))
