(ns minusthree.platform.sdl3.sdl3
  (:include
   "SDL3/SDL.h"
   "extra.hpp")
  (:require
   [minusthree.platform.sdl3.learn.learn01-triangle :refer [triangle-sdl]]))

(defn start
  ([] (start {}))
  ([{:keys [stop-atom]}]
   (triangle-sdl stop-atom)))
